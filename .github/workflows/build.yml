name: Build

on: push

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3

      - name: Check codestyle for sources
        run: git ls-files src/*.{c,h} | xargs clang-format -i --verbose && git diff --exit-code

      - name: Build application
        run: make